# Legacy Knowledge Files

This directory contains archived knowledge files from earlier versions.
Files here are not actively loaded by the knowledge system.

For current knowledge, see the root `knowledge/` directory and its subdirectories:
- `attack_chains/` — Multi-step exploitation paths
- `binary/` — Binary exploitation techniques
- `cloud/` — Cloud platform exploitation
- `enterprise/` — Enterprise security standards
- `payloads/` — Language/technology-specific payloads
- `web/` — Web vulnerability payloads
- `windows/` — Windows/AD exploitation
